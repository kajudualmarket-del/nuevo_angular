/* frontend/tsconfig.app.json (CORRECCIÓN FINAL) */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": [],
    
    // 🚀 CORRECCIÓN CLAVE: Agregamos las opciones de alias
    "baseUrl": "./", 
    "paths": {
      "src/*": ["src/*"] 
    }
  },
  "include": [
    "src/**/*.ts"
  ],
  "exclude": [
    "src/**/*.spec.ts"
  ]
}